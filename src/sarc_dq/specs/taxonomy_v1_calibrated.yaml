# AUTO-GENERATED by scripts/calibrate_taxonomy.py — do not edit by hand.
# Taxonomy v1: every parameter carries a provenance block.
# provenance.type: computed | literature | default (default => flagged).
version: v1
generated: 2026-07-07
classes:
  stale_master_data:
    default_rate:
      value: 0.1
      v0_default: 0.1
      provenance:
        type: "default"
        flagged: true
        note: "staleness prevalence is source-specific; grid brackets the governed band"
    min_age_days:
      value: 90
      v0_default: 90
      provenance:
        type: "default"
        flagged: true
        pending_source_dataset: "ALFRED archival vintages"
        note: "awaiting ALFRED archival vintages (tier2-validation run); fit the staleness window to real revision-lag distributions"
    max_age_days:
      value: 180
      v0_default: 180
      provenance:
        type: "default"
        flagged: true
        pending_source_dataset: "ALFRED archival vintages"
        note: "awaiting ALFRED archival vintages (tier2-validation run); fit the staleness window to real revision-lag distributions"
  superseded_golden_record:
    default_rate:
      value: 0.05
      v0_default: 0.05
      provenance:
        type: "default"
        flagged: true
        note: "no public per-type base rate; scaffolding default"
  silent_unit_change:
    default_rate:
      value: 0.05
      v0_default: 0.05
      provenance:
        type: "default"
        flagged: true
        note: "public base-rate evidence absent for this class; declared default"
  duplicate_vendor_conflicting_terms:
    default_rate:
      value: 0.05
      v0_default: 0.05
      provenance:
        type: "default"
        flagged: true
        pending_source_dataset: "Magellan/DeepMatcher (Amazon-Google, Walmart-Amazon, DBLP-Scholar)"
        note: "awaiting Magellan/DeepMatcher (Amazon-Google, Walmart-Amazon, DBLP-Scholar) (tier2-validation run); fit duplicate density from matched-pair attribute disagreement"
  cross_source_contradiction:
    default_rate:
      value: 0.05
      v0_default: 0.05
      provenance:
        type: "literature"
        citation: "li2013"
        note: "sources conflict on ~70% of items on the deep web (Li et al. 2013) — an upper anchor; the governed-source default stays low and flagged"
    tolerance:
      value: 0.02
      v0_default: 0.02
      provenance:
        type: "default"
        flagged: true
        note: "agreement tolerance is domain-specific; scaffolding default"
  schema_drift:
    default_rate:
      value: 0.05
      v0_default: 0.05
      provenance:
        type: "literature"
        citation: "curino2013"
        note: "171 schema versions in ~4.5y (Curino et al.) anchors drift cadence, not a per-record rate; default stays flagged"
  missing_mandatory_field:
    default_rate:
      value: 0.05
      v0_default: 0.05
      provenance:
        type: "default"
        flagged: true
        pending_source_dataset: "Raha/Baran (Hospital, Flights, Beers, Rayyan, Movies, Tax)"
        note: "awaiting Raha/Baran (Hospital, Flights, Beers, Rayyan, Movies, Tax) (tier2-validation run); fit to labeled missing-value cell rates"
  plausible_outlier:
    default_rate:
      value: 0.05
      v0_default: 0.05
      provenance:
        type: "default"
        flagged: true
        note: "public base-rate evidence absent for this class; declared default"
