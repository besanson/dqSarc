name: exp-ablations

# Manually triggered LIVE experiment kit (Ablation (each predicate off)). Real Claude — costs money.
# Predictions are pre-registered in reports/prereg/ablations.md (frozen before any run).
# Orchestration only; no experiment code, prompts, or config changed by this file.
# Results -> results/ablations-live. Requires an ANTHROPIC_API_KEY repository secret.
on:
  workflow_dispatch:
    inputs:
      episodes:
        description: "Episodes per condition"
        required: true
        default: "100"

permissions:
  contents: write

jobs:
  live:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check ANTHROPIC_API_KEY secret is present
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          if [ -z "$ANTHROPIC_API_KEY" ]; then
            echo "::error::ANTHROPIC_API_KEY repository secret is not set."
            exit 1
          fi
          echo "ANTHROPIC_API_KEY is present."
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install
        run: pip install -e ".[dev,live,gate]"
      - name: Seed resume from prior partial results (if any)
        run: |
          mkdir -p reports/exp
          git fetch origin "results/ablations-live" --depth=1 2>/dev/null || true
          git show "origin/results/ablations-live:reports/exp/ablations_summary.json" \
            > "reports/exp/ablations_summary.json" 2>/dev/null \
            && echo "resuming from prior partial results" \
            || echo "fresh run (no prior partial)"
      - name: Run experiment ablations (LIVE)
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          EPISODES: ${{ inputs.episodes }}
        run: |
          mkdir -p reports/exp
          python -m benchmarks.experiments --exp ablations --arm live \
            --episodes "$EPISODES" --concurrency 8 --max-minutes 320 \
            --out reports/exp/ablations_summary.json
      - name: Upload experiment artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: exp-ablations-results
          path: reports/exp/
          if-no-files-found: error
      - name: Commit results to results/ablations-live
        env:
          EPISODES: ${{ inputs.episodes }}
          RUN_ID: ${{ github.run_id }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -B results/ablations-live
          git add -f reports/exp/ablations_summary.json
          if git diff --cached --quiet; then echo "no changes"; exit 0; fi
          git commit -m "ablations live results (${EPISODES} episodes, run ${RUN_ID})"
          git push --force origin results/ablations-live
